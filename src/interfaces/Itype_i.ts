export interface Itype_i{
    length?: number;
    bits?: number[];
    instruction: string | undefined,
    format: string,
    op: number | undefined,
    rs: number | undefined,
    rt: number | undefined,
    immediate: number | undefined
}